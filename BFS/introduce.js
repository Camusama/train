// BFS（广度优先搜索） 常用来解决最短路径问题。

// 第一次便利到目的节点时，所经过的路径是最短路径。

// 几个要点：

// 只能用来求解无权图的最短路径问题
// 队列：用来存储每一层便利得到的节点
// 标记：对于遍历过的结点，应将其标记，以防重复访问
BFS()
function BFS() {
       queue<int> q;//初始化队列Q 
       while(!q.empty())  //队列不为空
       {
               if() //判断是否找到了目标
               {

               }
               //队首出队
               for()
               {
                       //依旧是四个方向
                       //符合条件的入队
                       //标记入队的点
               }
       }
}
